parameters:
  - name: stage
    type: string
  - name: targets
    type: object

  - name: jobFormat
    type: string
    default: "{0}"
  - name: nameFormat
    type: string
    default: "{0}"
  - name: testFormat
    type: string
    default: "{0}"

stages:
  - template: test-unversioned-grouped.yml
    parameters:
      stage: ${{ parameters.stage }}
      targets: ${{ parameters.targets }}
      jobFormat: ${{ parameters.jobFormat }}
      nameFormat: ${{ parameters.nameFormat }}
      testFormat: ${{ parameters.testFormat }}
      groups:
        - ''
